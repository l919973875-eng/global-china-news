# 第一次只做这 6 步

1. GitHub 新建一个名为 `global-china-news` 的 **Public** 仓库。
2. Windows 右键压缩包 → **全部解压**。
3. GitHub 仓库 → **Add file → Upload files** → 把解压目录里的全部文件/文件夹拖进去 → **Commit changes**。
4. GitHub 仓库 → **Settings → Pages → Source → GitHub Actions**。
5. GitHub 仓库 → **Settings → Secrets and variables → Actions → New repository secret**，名称填 `OPENAI_API_KEY`，值填你的 API Key（没有可以先跳过）。
6. GitHub 仓库 → **Actions → 全球广义涉华新闻云端抓取 → Run workflow**。运行成功后去 **Settings → Pages → Visit site**。

以后电脑不用开着，GitHub 云端每 6 小时自动把 311 个来源全部巡检一遍。

> 注意：GitHub Pages 默认是公开网页。如果你不希望广义涉华筛选结果公开，不要开启 Pages，改用私有模式。
